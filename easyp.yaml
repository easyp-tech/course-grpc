generate:
  inputs:
    - directory: "api"
  plugins:
    - name: go
      out: ./pkg
      opts:
        paths: source_relative
    - name: go-grpc
      out: ./pkg
      opts:
        paths: source_relative
        require_unimplemented_servers: false
